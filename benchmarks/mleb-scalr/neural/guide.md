# MLEB-SCALR — Neural (Graph Search)

## Dataset

Same as core — see [core/guide.md](../core/guide.md) for dataset details.

## Running

```bash
npx tsx --env-file=.env mleb-scalr/neural/run.ts --seed
npx tsx --env-file=.env mleb-scalr/neural/run.ts
```

## What it measures

- Graph-augmented retrieval on legal holdings
- Whether entity/relationship extraction from legal text improves cross-domain retrieval
